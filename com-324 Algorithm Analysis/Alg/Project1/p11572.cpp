#include <iostream>
#include <unordered_map>
#include <set>
#include <vector>

using namespace std;

int main() {
	int t;
	while (cin >> t) {
		while (t--) {
			int n; cin >> n;
			unordered_map<int, set<int>> m;
			vector<int> v;
			set<int> s;
			while (n--) {
				int snow; cin >> snow;
				v.push_back(snow);
			}
			
			for (int i = int(v.size()-1); i) {
				for (int i = 0;) {
					if () {
						
					}
				}
			}
		}
	}
	
	return 0;
}