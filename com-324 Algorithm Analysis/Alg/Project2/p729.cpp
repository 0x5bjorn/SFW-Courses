#include <iostream>
#include <string>
#include <vector>

using namespace std;

void solve(string s, vector<string> v) {
	stack<string> st;
	
	while (!st.empty()) {
		st.push_back(s);
		
		for (int i = 0; i<n; ++i) {
			
		}
		
		
	}
}

int main() {
	int t; cin >> t;
	while (t--) {
		cin >> n >> h;
		vector<string> v;
		string s;
		for (int i = 0; i<n; ++i) {
			s += "0";
		}
		
	}
	
	return 0;
}